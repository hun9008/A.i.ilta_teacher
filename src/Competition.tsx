import React from 'react';
import styles from './css/MainPage.module.css';

const Competition: React.FC = () => {
  return (
    <div className={styles.mainContent}>
      <h1>File Text Page</h1>
      {/* Add content for FileText page */}
    </div>
  );
};

export default Competition;
